**To do list**
mark tasks as done by adding an "x" between the brackets, like this: [x]
- [ ] choose a pretrained model and implement it(this will be the initial global model)
- [x] gather data (face images for clients)
 data/
   student_1/
   student_2/
   student_3/
- [ ] implementation using flower: server(aggregation function) and client(custom training loop)
- .......